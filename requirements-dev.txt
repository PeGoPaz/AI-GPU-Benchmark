# Everything needed to work on the project and run the test suite.
#
# Deliberately without torch and the Hugging Face stack: tests/conftest.py
# replaces those with stubs, so installing them would add gigabytes without
# covering a single extra line. This is also exactly what CI installs.
#
# Ruff is pinned to the version CI enforces (keep in sync with
# .github/workflows/ci.yml) — an unpinned local ruff can disagree with the
# pipeline over rules that changed between releases.

pytest>=8.0.0
ruff==0.16.5

# Imported for real by the tests, unlike the ML stack.
PyQt6>=6.6.0
nvidia-ml-py>=12.0.0
pandas>=2.2.0
matplotlib>=3.8.0
