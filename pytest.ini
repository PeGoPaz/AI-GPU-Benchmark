[pytest]
testpaths = tests
# Make `import app.*` work without installing the project.
pythonpath = .
addopts = -q --strict-markers
markers =
    ui: requires a Qt platform plugin; skipped automatically when unavailable
